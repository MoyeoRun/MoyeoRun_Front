import { Box } from 'native-base';
import React from 'react';

type DetailedRecordProps = {};

const DetailedRecord = ({}: DetailedRecordProps) => {
  return <Box>기록</Box>;
};

export default DetailedRecord;
